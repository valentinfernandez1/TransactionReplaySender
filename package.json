{
  "name": "store-tx-sender",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "build": "rm -rf dist && tsc --project tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist/index.js",
    "dev": "npm run build && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@polkadot/api": "^10.9.1",
    "dotenv": "^16.3.1",
    "ts-node": "^10.7.0",
    "mongodb": "^4.11.0",
    "mongoose": "^7.1.0"
  },
  "devDependencies": {
    "typescript": "^4.6.4"
  }
}
